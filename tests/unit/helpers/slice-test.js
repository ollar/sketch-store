
import { slice } from 'sketch-shop/helpers/slice';
import { module, test } from 'qunit';

module('Unit | Helper | slice');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = slice([42]);
  assert.ok(result);
});


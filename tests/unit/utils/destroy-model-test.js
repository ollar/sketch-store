import destroyModel from 'sketch-shop/utils/destroy-model';
import { module, test } from 'qunit';

module('Unit | Utility | destroy model');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = destroyModel();
  assert.ok(result);
});

import showdown from 'sketch-shop/utils/showdown';
import { module, test } from 'qunit';

module('Unit | Utility | showdown');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = showdown();
  assert.ok(result);
});

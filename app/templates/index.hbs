{{#swiper-container class="categories full-width"
  fullHeight=true
  options=(hash
      effect='slide'
      parallax=true
      slidesPerView=1
  )
}}
  {{#each model.categories as |category|}}
      {{category-slide category=category}}
  {{/each}}
{{/swiper-container}}
